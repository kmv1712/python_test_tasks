HTTP, IP, TCP
#
# http базируется на tcp и ip
#
# ip() между двумя машинами тонель передачи данных, данные передаются пакетами,
# проблема данные могут повредится или не прийти в правильном порядке, т.к. ip оказался не очень надежным
# был создан транспортный протокол tcp, превносит порядок в хаусе передачи данных по протоколу ip
# tcp превносит в конструкцию порт, порты нужны, чтобы несколько приложений могло использовать
# tcp на одной машине не занимая собой весь тунель.
# Ленина д.1. это адрес ip
# а порты это квартиры
#
# пара ip address:5000 - называется socket(мостик между субъектами)
# INET протокол ip 4 версии
